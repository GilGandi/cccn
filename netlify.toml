[build]
  command = "rm -f middleware.ts && npm install --legacy-peer-deps && npx prisma generate && npx next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
